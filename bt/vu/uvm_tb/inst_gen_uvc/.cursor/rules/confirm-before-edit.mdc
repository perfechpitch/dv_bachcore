---
description: 修改代码前必须弹出同意/不同意按钮，用户确认后才可动手
alwaysApply: true
---

# 修改前一键确认

在**提议修改任何代码或配置文件**之前，必须先用 **AskQuestion** 工具向用户确认，不得直接编辑。

## 流程

1. 先说明：改哪些文件、改什么、为什么
2. 调用 AskQuestion，提供两个固定选项：
   - **同意修改**（id: `agree`）
   - **不同意**（id: `disagree`）
3. 仅当用户选择 **同意修改** 后，才可使用 Write / StrReplace 等编辑工具
4. 若选择 **不同意**：停止修改，可询问用户希望如何调整方案

## AskQuestion 示例

```json
{
  "title": "是否同意以下修改？",
  "questions": [{
    "id": "confirm_edit",
    "prompt": "简要列出拟修改的文件与变更内容",
    "options": [
      { "id": "agree", "label": "同意修改" },
      { "id": "disagree", "label": "不同意" }
    ]
  }]
}
```

## 例外（无需再次确认）

- 用户消息中已明确写出「同意」「可以改」「按方案执行」等授权
- 纯问答、代码解释、review，不涉及文件编辑
- 用户明确要求「直接改，不用再问」
